# Администрирование

Доступ: `ADMIN`, `DIRECTOR`, `OWNER`.

Раздел `/admin`:

- пользователи: создание, роль, подразделение, вкл/выкл;
- классификаторы: причины потерь, причины отмены, каналы, направления услуг.

Демо: `admin@clinic.local` / `demo1234` или `owner@clinic.local`.
